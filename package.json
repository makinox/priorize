{
  "name": "priorize",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17",
    "react-dom": "^17",
    "react-scripts": "4"
  },
  "scripts": {
    "cy-test": "start-server-and-test serve http://localhost:3000 cy-run",
    "test": "start-server-and-test start http://localhost:3000 cy-open",
    "serve": "serve build --single -p 3000",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "cy-open": "cypress open",
    "cy-run": "cypress run"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "plugin:cypress/recommended"
    ]
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "cypress": "^6.2.1",
    "eslint-plugin-cypress": "^2.11.2",
    "serve": "^11.3.2",
    "start-server-and-test": "^1.11.7"
  }
}
